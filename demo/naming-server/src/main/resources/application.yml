spring:
  application:
    name: eureka-server
server:
  port: ${SERVER_PORT:8761}
management:
  endpoints:
    web:
      exposure:
        include: health,info
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
