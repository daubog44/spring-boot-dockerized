# log dei servizi avviati con `task dev`
.dev-logs/

# cartella prodotta da `task consegna`
consegna/

# il video del corso lo rigenera corso/genera-video.ps1: sta nella release, non nel repository
corso/*.mp4

# il testo del corso lo raccoglie task learn dalle lezioni e dalle guide
corso/contenuti.js

# il nome del progetto Docker Compose di questa copia: lo scrive dev-lib
.env

# la copia di scorta di task che scarica task offline-prep: e' per questa
# macchina (un eseguibile per sistema operativo), non per il repository
.tools/
